// Global app config. Replace this file at build/deploy time to inject production values.
window.APP_CONFIG = window.APP_CONFIG || {
  // API base (including /api path)
  BASE_URL: 'http://localhost:8080/api',
};

// Example: your CI can overwrite this file with production values before deployment.
